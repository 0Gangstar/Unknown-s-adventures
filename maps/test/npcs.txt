

name: niga  {
    replics = r, r2
    template = test  
    spawn(10, 5)     
    stand(right)
    set_speed(500)
    walk(12)
    stand(down) 
    walk(4)
}


name: uhh_artem {
    template = test  
    replics = r4, r5
    spawn(6, 8)     
    stand(right)
    wait(2)
    set_speed(300)
    walk(10)
    stand(down)
    set_speed(150)
    walk(8)
    stand(left)
}